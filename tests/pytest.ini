[pytest]
testpaths = .
# rootdir is deliberately this folder: the pack directory name contains a
# hyphen, so pytest must never collect it as a package (its __init__.py would
# be imported as a bare module and its relative imports would fail).
# Run from the ComfyUI root:  python -m pytest custom_nodes/NH-Nodes/tests -q
